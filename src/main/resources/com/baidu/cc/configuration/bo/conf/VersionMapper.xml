<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baidu.cc.configuration.dao.VersionMapper">

  <select id="findByName" resultMap="BaseResultMap" parameterType="java.lang.Long">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select 
    <include refid="Base_Column_List"/>
    from version
    where name = #{name}
  </select>

  <select id="findVersionListByEnvId" resultMap="BaseResultMap" parameterType="java.lang.Long">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select
    <include refid="Base_Column_List"/>
    from version
    where environment_id = #{environmentId}
  </select>

  <delete id="deleteVersionCascadeByEnvIds" parameterType="java.lang.Long">
      delete from version where environment_id in
      <foreach item="envId" collection="envIds" open="("
               separator="," close=")">
          #{envId, jdbcType=INTEGER}
      </foreach>
  </delete>

  <select id="findVersionIdsByEnvIds" parameterType="java.lang.Long" resultType="java.lang.Long">
      select id from version where environment_id in
      <foreach item="envId" collection="envIds" open="("
               separator="," close=")">
          #{envId, jdbcType=INTEGER}
      </foreach>
  </select>
  
 <select id="findLastestByEnvId" resultMap="BaseResultMap" parameterType="java.lang.Long">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select 
    <include refid="Base_Column_List"/>
    from version
    where environment_id = #{envId} order by id desc limit 1
  </select>

  <select id="findVersionListByEnvIds" resultMap="BaseResultMap" parameterType="java.util.List">
      select
      <include refid="Base_Column_List"/> 
      from version where environment_id in
      <foreach item="item" index="index" collection="envIds" open="(" separator="," close=")">#{item}</foreach>
  </select>
</mapper>